name: Validate Claude plugin

"on":
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "17 7 * * 1"

permissions:
  contents: read

concurrency:
  group: validate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
        with:
          node-version: 24
      - name: Install pinned Claude Code validator
        run: npm install --global @anthropic-ai/claude-code@2.1.241
      - name: Run structural checks
        run: python3 scripts/validate.py
      - name: Validate plugin strictly
        run: claude plugin validate . --strict
      - name: Verify loaded component inventory
        run: claude --plugin-dir . plugin details clicktrail

  latest-validator-advisory:
    if: github.event_name == 'schedule'
    continue-on-error: true
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
        with:
          node-version: 24
      - name: Install current Claude Code validator
        run: npm install --global @anthropic-ai/claude-code@latest
      - name: Check compatibility with current validator
        run: claude plugin validate . --strict
